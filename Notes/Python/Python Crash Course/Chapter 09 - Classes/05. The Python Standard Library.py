# The Python standard library is a set of modules included with every Python installation 
# Functions from the random module: randint(), choice(), etc 

# Syntax 
import random 

# or 

from random import function_name
