"""
When you create an attribute using self in a Python class, that attribute becomes available to all methods within that class. 
This is because self refers to the instance of the class, and any attribute attached to it persist throughout the object's lifetime.
"""
